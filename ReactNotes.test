React Element :  React every element as object there element id and classNmae pass as props 
#root: #root id Inside react overwrite elemnt content
render method: Render method overwrite dom node 

React is react: React js develop by facebook it's just a library Since React is a front-end framework or the “View” in MVC,
    this means that as the user clicks around and changes the app’s data, the view should “react” or change with those user events. 
    User events being mouse clicks, typing, submitting a form.  

React and ReactDom : The reason React and ReactDOM were split into two libraries was due to the arrival of React Native 
                    (A react platform for mobile development). React components are such a great way to organize
                     UI that it has now spread to mobile to react is used in web and in mobile. react-dom is used only in web apps.